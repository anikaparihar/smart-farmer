# models/

This folder is auto-populated when you run `python src/train.py`.

Files generated here:
- `crop_model.pkl`          — Trained model + label encoder
- `confusion_matrix.png`    — Evaluation heatmap
- `pairplot.png`            — Joint distribution plot
- `correlation_heatmap.png` — Feature correlation heatmap
- `feature_importance.png`  — Bar chart of feature importances
- `decision_tree.png`       — Visual decision tree diagram
